Initial data from the RWB

How to download and view the data:
MacOS Terminal:

brew install gdal \
gdal-config --version \
pip install pyrosm \
OR \
pip install osmium

Т.е. главное — поставить зависимость через Homebrew.
